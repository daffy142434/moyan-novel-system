# Codex AI — 智能编程助手 · 产品需求文档（PRD）

> **版本**: v1.0  
> **日期**: 2026-07-14  
> **状态**: Draft  

---

## 目录

1. [产品概述](#1-产品概述)
2. [市场与竞品分析](#2-市场与竞品分析)
3. [用户画像与场景](#3-用户画像与场景)
4. [功能需求](#4-功能需求)
5. [非功能需求](#5-非功能需求)
6. [产品路线图](#6-产品路线图)
7. [商业模式](#7-商业模式)
8. [附录](#8-附录)

---

## 1. 产品概述

### 1.1 产品定位

**Codex AI** 是一款 **终端原生 + Web UI** 双模式的 AI 编程助手。它不是一个简单的代码补全工具，而是一个能够**理解完整代码库、自主规划任务、跨文件编辑、执行命令并完成测试与部署的全链路 AI 开发伙伴**。

一句话定位：**"你在终端里的 AI 结对编程搭档，从理解项目到交付代码，你说需求，它干活。"**

### 1.2 产品愿景

让每一位开发者都能拥有一个 7×24 小时在线的 AI 开发伙伴——不挑语言、不挑框架、不挑 IDE，在任何工作环境中都能提供**项目级理解、自主执行、迭代验证**的能力，将开发者从重复编码中解放出来，聚焦架构设计与产品决策。

### 1.3 核心价值主张

| 维度 | 描述 |
|------|------|
| **项目级理解** | 不止看当前文件，能理解整个项目的结构、依赖关系、编码约定 |
| **自主代理** | 给定自然语言任务，Agent 自主规划→执行→验证→迭代 |
| **多模态交互** | CLI 终端 + Web UI + IDE 插件，覆盖所有开发场景 |
| **安全可控** | 每次修改有确认、可回滚、可审计，关键操作必须经过开发者批准 |
| **模型中立** | 支持多模型后端（Claude、GPT、DeepSeek），用户自由切换 |

---

## 2. 市场与竞品分析

### 2.1 市场现状

2025-2026 年，AI 编程助手市场经历了从"代码补全"到"自主代理"的范式转变。截止 2026 年中：

- **Claude Code** 年化营收超 25 亿美元，SWE-bench 达 80.9%
- **GitHub Copilot** 拥有最大用户基数，推出 Cloud Agent 实现异步自主编程
- **Cursor** 以 AI 原生 IDE + 多 Agent 并行 + 云端 sub-agent 构建竞争壁垒
- **Windsurf / Codeium / Trae** 等产品也在快速迭代

### 2.2 竞品对比

| 维度 | Claude Code | GitHub Copilot | Cursor | **Codex AI (本项目)** |
|------|------------|----------------|--------|----------------------|
| **交互模式** | 终端 CLI | IDE 插件 + CLI + Cloud | AI 原生 IDE | **终端 CLI + Web UI + IDE 插件** |
| **Agent 能力** | 强（完整的 Agent Loop） | 强（Agent Mode + Cloud Agent） | 强（多 Agent 并行） | **强（三层 Agent 架构）** |
| **上下文窗口** | 200K tokens | 受限于模型 | 受限于模型 | **多 Agent + 分片上下文管理** |
| **模型支持** | 仅 Anthropic | 自研 Polaris + GPT | 自研 Composer + 多模型 | **模型中立（多后端）** |
| **安全机制** | 权限确认 | PR Review + Sandbox | 本地沙箱 | **分层权限 + 沙箱执行** |
| **扩展性** | MCP 协议 | MCP 协议 | MCP + Rules | **MCP + Skill 双扩展** |
| **开源** | 否 | 否 | 否 | **开源核心（社区版）** |

### 2.3 差异化策略

1. **双模交互**：同时提供终端 CLI（效率优先）和 Web UI（可视化优先），覆盖不同用户偏好
2. **模型中立**：用户可以选择 Claude、GPT、DeepSeek 等任何兼容模型，不被锁定
3. **开源核心**：社区版开源，吸引开发者生态；企业版提供私有部署和管理能力
4. **三层上下文工程**：短程（当前对话）→ 中程（项目索引）→ 长程（跨项目知识），高效利用 token
5. **Skill 系统**：类似 MCP 但更面向代码库——项目级 Skill 复用开发规范和最佳实践

---

## 3. 用户画像与场景

### 3.1 用户画像

| 角色 | 描述 | 核心需求 |
|------|------|---------|
| **独立开发者** | 个人开发者，全栈工作 | 快速搭建项目、减少重复编码、调试辅助 |
| **创业团队** | 3-10 人小团队，节奏快 | 代码生成、重构、多文件操作、CI 集成 |
| **企业开发团队** | 中大型团队，有规范和流程 | 安全合规、项目管理、知识沉淀、团队协作 |
| **非技术用户** | 产品经理/设计师偶尔改代码 | 自然语言描述需求，自动生成可运行代码 |
| **技术管理者** | Tech Lead / CTO | 代码审查、架构分析、项目健康度评估 |

### 3.2 核心使用场景

#### 场景 A：新项目脚手架
> "帮我创建一个 React + TypeScript + Tailwind 的项目，配置好 ESLint 和 Prettier，用 pnpm workspace 管理"

→ Codex 自动执行 `npm create vite`、安装依赖、生成配置文件、验证项目可运行

#### 场景 B：跨文件重构
> "把用户认证模块从 session 改造成 JWT 方案，更新所有相关文件"

→ Codex 理解当前认证逻辑、规划重构步骤、修改多个文件、更新测试、验证构建通过

#### 场景 C：Bug 修复
> "登录页面报 401 错误，帮我查一下原因"

→ Codex 阅读错误堆栈、追踪请求链路、定位 bug 位置、修复代码、验证修复

#### 场景 D：代码审查
> "Review 这个 PR，#42"

→ Codex 检查代码质量、安全漏洞、性能问题、提出改进建议，自动运行测试

#### 场景 E：文档生成
> "给这个 API 模块生成完整的 API 文档，包括参数说明和示例"

→ Codex 分析代码结构、提取 JSDoc/类型定义、生成 Markdown 或 OpenAPI 文档

#### 场景 F：技术学习
> "解释这个 k8s deployment.yaml 每一行的作用"

→ Codex 逐行解析配置文件并给出通俗解释

---

## 4. 功能需求

### 4.1 功能全景图

```
┌─────────────────────────────────────────────────────────┐
│                    Codex AI 功能全景                       │
├─────────────────────────────────────────────────────────┤
│  1. 交互层                                              │
│     ├── 终端 CLI（REPL 模式 + 单次命令模式）               │
│     ├── Web UI（聊天界面 + 文件浏览器 + Diff 预览）        │
│     └── IDE 插件（VS Code / JetBrains）                  │
├─────────────────────────────────────────────────────────┤
│  2. Agent 核心                                           │
│     ├── 代理循环（Agent Loop）：思考→行动→观察→迭代        │
│     ├── 三种工作模式：默认（确认）→ 快速（自动）→ 规划      │
│     ├── 子代理（Sub-agent）：并行处理独立子任务             │
│     └── 检查点（Checkpoint）：回滚到任意历史状态            │
├─────────────────────────────────────────────────────────┤
│  3. 工具系统                                              │
│     ├── 文件操作：读/写/编辑/搜索/列目录                   │
│     ├── 命令执行：Shell 命令 / 脚本运行                   │
│     ├── 代码搜索：语义搜索 / 正则搜索 / 文件名搜索          │
│     ├── Git 操作：状态/提交/分支/PR                         │
│     ├── LSP 集成：跳转定义/查找引用/类型信息               │
│     └── Web 搜索：联网获取最新信息                        │
├─────────────────────────────────────────────────────────┤
│  4. 上下文与记忆                                          │
│     ├── 项目索引：向量化索引整个代码库                      │
│     ├── 项目配置（CODEX.md）：编码规范/命令偏好             │
│     ├── 对话记忆：短期上下文管理                            │
│     └── 知识沉淀：项目级最佳实践自动积累                    │
├─────────────────────────────────────────────────────────┤
│  5. 安全与权限                                            │
│     ├── 分层权限模型：文件/命令/网络逐级控制               │
│     ├── 操作确认：敏感操作需开发者批准                      │
│     ├── 操作审计：完整日志记录                              │
│     └── 沙箱执行：隔离环境运行不可信代码                    │
├─────────────────────────────────────────────────────────┤
│  6. 扩展生态                                              │
│     ├── MCP 协议兼容：接入外部工具和数据源                  │
│     ├── Skill 市场：社区贡献的即用技能包                   │
│     ├── 自定义工具：用户注册自己的工具                      │
│     └── 模型适配器：切换任意兼容模型                       │
├─────────────────────────────────────────────────────────┤
│  7. 企业特性                                              │
│     ├── 私有化部署：本地/私有云                            │
│     ├── 团队协作：共享配置/评审流程                        │
│     ├── 权限管理：RBAC 集成                               │
│     ├── 审计日志：所有操作可追溯                            │
│     └── SSO 集成：企业统一登录                            │
└─────────────────────────────────────────────────────────┘
```

### 4.2 功能详细说明（按优先级）

#### P0 - 核心功能（MVP 必须）

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F-01 | **终端 REPL** | 交互式终端界面，用户输入自然语言指令，AI 实时响应 | P0 |
| F-02 | **单次命令模式** | `codex "任务描述"` 非交互式执行 | P0 |
| F-03 | **文件读取** | 读取文件内容，支持行号显示，支持图片/PDF | P0 |
| F-04 | **文件写入** | 创建新文件 | P0 |
| F-05 | **文件编辑** | 精确字符串替换编辑，支持 diff 显示 | P0 |
| F-06 | **Shell 执行** | 运行终端命令，实时输出显示，支持超时控制 | P0 |
| F-07 | **文件名搜索** | 通过 glob 模式匹配文件名 | P0 |
| F-08 | **内容搜索** | 通过正则表达式搜索文件内容 | P0 |
| F-09 | **Agent Loop** | ReAct 模式的核心循环：思考→调用工具→观察结果→继续 | P0 |
| F-10 | **权限确认** | 执行文件修改和命令前向用户请求确认 | P0 |
| F-11 | **项目上下文** | 自动读取项目结构、配置文件，理解项目基本信息 | P0 |
| F-12 | **多模型支持** | 支持 Claude / GPT / DeepSeek 切换 | P0 |

#### P1 - 重要功能（V1.1）

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F-13 | **Web UI** | 浏览器界面，含聊天面板、文件树、Diff 预览 | P1 |
| F-14 | **三种工作模式** | 默认（确认）/ 快速（自动）/ 规划（先出计划） | P1 |
| F-15 | **项目配置（CODEX.md）** | 项目级配置文件，定义规范、命令、忽略规则 | P1 |
| F-16 | **操作回滚** | Checkpoint 检查点，可回滚到任意历史状态 | P1 |
| F-17 | **Git 操作** | Git status / diff / commit / branch 操作 | P1 |
| F-18 | **代码语义搜索** | 基于向量 embedding 的代码语义搜索 | P1 |
| F-19 | **Web 搜索** | 联网搜索获取最新 API 文档和解决方案 | P1 |
| F-20 | **SSE 流式输出** | 工具调用和生成过程实时流式展示 | P1 |

#### P2 - 增强功能（V1.2）

| ID | 功能 | 描述 | 优先级 |
|----|------|------|--------|
| F-21 | **LSP 集成** | 语言服务器协议集成，支持跳转定义、查找引用 | P2 |
| F-22 | **子代理** | 复杂任务拆分为多个子 Agent 并行处理 | P2 |
| F-23 | **MCP 扩展** | 兼容 Model Context Protocol，接入外部工具 | P2 |
| F-24 | **Skill 系统** | 可复用的自动化技能包（如"创建 React 组件模板"） | P2 |
| F-25 | **代码审查** | 自动 PR 审查，发现 bug、安全问题、风格问题 | P2 |
| F-26 | **自动化任务** | 定时/事件触发任务（如每日依赖更新检查） | P2 |

#### P3 - 未来功能（V2.0+）

| ID | 功能 | 描述 | 优先级 |
|----|------|--------|--------|
| F-27 | **IDE 插件** | VS Code / JetBrains 插件 | P3 |
| F-28 | **Cloud Agent** | 云端异步执行任务，关闭电脑后继续运行 | P3 |
| F-29 | **团队协作** | 共享配置、协作评审、知识库 | P3 |
| F-30 | **企业部署** | 私有化部署、SSO、RBAC、审计 | P3 |

---

## 5. 非功能需求

### 5.1 性能

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **首次响应时间** | < 2s | 用户输入后 AI 开始生成回复的时间 |
| **流式输出延迟** | < 100ms/token | 每个 token 的生成延迟 |
| **文件读取** | < 500ms | 读取 1MB 以内文件 |
| **文件搜索** | < 1s | 10 万文件以内项目，文件名/内容搜索 |
| **代码语义搜索** | < 2s | 首次搜索含索引时间 |
| **命令执行超时** | 默认 120s | 可通过参数配置 |
| **上下文窗口** | 支持 ≥ 200K tokens | 大型项目上下文管理 |
| **并发会话** | ≥ 10 | 同时运行多个独立对话 |

### 5.2 安全

| 要求 | 描述 |
|------|------|
| **文件操作保护** | 写操作限制在工作目录内，越界需确认 |
| **命令执行保护** | 阻止高危命令（rm -rf /, dd 等），可配置白名单 |
| **沙箱执行** | 可选 Docker 沙箱运行不可信代码 |
| **审计日志** | 所有操作（工具调用、文件变更、命令执行）完整记录 |
| **API 密钥安全** | 模型 API Key 加密存储，不透传 |
| **隐私** | 代码数据可选不发送到第三方，支持本地模型 |

### 5.3 可用性

| 要求 | 描述 |
|------|------|
| **跨平台** | 支持 macOS / Linux / Windows |
| **安装复杂度** | 一行命令安装，零配置即可使用 |
| **离线能力** | 支持本地模型（Ollama），断网可用部分功能 |
| **升级** | 自动检查更新，热升级 |
| **错误处理** | 友好的错误提示，明确的修复建议 |
| **中文支持** | 全界面/文档中文支持，自然语言中英文混合输入 |

### 5.4 可扩展性

| 要求 | 描述 |
|------|------|
| **模型无关** | 插拔式模型适配器，支持 Claude / GPT / DeepSeek / Ollama |
| **工具扩展** | 用户可注册自定义工具，实现任意 CLI 工具调用 |
| **MCP 兼容** | 兼容 MCP 协议实现，可接入任意 MCP Server |
| **插件系统** | 社区插件市场，按需安装功能扩展 |

---

## 6. 产品路线图

### Phase 0：技术验证（2 周）
- 实现最小 Agent Loop（约 500 行核心代码）
- 基础工具：read / write / edit / bash / glob / grep
- 终端 REPL 界面
- 单模型支持（DeepSeek / Claude 任选）

### Phase 1：MVP 发布（6-8 周）
- 完整的终端 CLI 体验（REPL + 单次命令）
- 全部 P0 功能
- 项目上下文感知
- 权限确认系统
- 两种模型后端切换
- 基础检查点/回滚功能

### Phase 2：Web UI + 增强（8-12 周）
- Web UI 界面发布
- 三种工作模式（默认 / 快速 / 规划）
- 操作回滚（完整 Checkpoint）
- Git 操作
- 语义搜索
- Web 搜索
- CODEX.md 项目配置
- SSE 流式输出

### Phase 3：生态与扩展（12-16 周）
- MCP 协议支持
- Skill 系统
- LSP 集成
- 子代理并行处理
- 代码审查能力
- 自动化任务

### Phase 4：企业版（16-24 周）
- IDE 插件（VS Code / JetBrains）
- Cloud Agent 云端异步执行
- 团队协作
- 私有化部署
- RBAC + SSO
- 审计日志 / 合规

---

## 7. 商业模式

### 7.1 版本策略

| 版本 | 价格 | 目标用户 | 主要限制 |
|------|------|---------|---------|
| **社区版（开源）** | 免费 | 个人开发者、爱好者 | 本地模型 + 自带 API Key |
| **Pro 版** | $20/月 | 独立开发者 | 云端模型额度、优先支持 |
| **Team 版** | $30/人/月 | 中小团队 | 团队协作、共享额度 |
| **Enterprise 版** | 定制报价 | 企业 | 私有部署、SSO、审计、SLA |

### 7.2 收入来源

1. **订阅付费**：Pro / Team / Enterprise 订阅
2. **API 代理**：提供统一 API 调用（按量计费，聚合多个模型供应商）
3. **私有化部署**：企业自托管 + 技术支持
4. **Skill 市场**：第三方技能分成

---

## 8. 附录

### 8.1 关键术语

| 术语 | 说明 |
|------|------|
| **Agent Loop** | 代理循环——AI 不断执行"思考→调用工具→观察结果"的迭代过程 |
| **ReAct** | Reasoning + Acting 的缩写，AI Agent 的核心范式 |
| **Tool Use** | 函数调用——模型通过调用预定义工具来操作外部环境 |
| **MCP** | Model Context Protocol，模型上下文协议，AI 与外部工具通信的开放标准 |
| **Checkpoint** | 检查点，保存 Agent 执行过程中的某个状态，支持回滚 |
| **SSE** | Server-Sent Events，服务端推送事件的流式传输协议 |

### 8.2 参考资源

- [Claude Code 架构](https://docs.anthropic.com/en/docs/claude-code)
- [GitHub Copilot Agent Mode](https://github.blog/news-insights/product-news/from-pair-to-peer-programmer-our-vision-for-agentic-workflows-in-github-copilot/)
- [Cursor Architecture](https://cursor.com)
- [MCP 协议规范](https://modelcontextprotocol.io)
- [OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling)

---

> **文档状态**: 初稿完成，待评审  
> **下一版本**: 收集反馈后更新
